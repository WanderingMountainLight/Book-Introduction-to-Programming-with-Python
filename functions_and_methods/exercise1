#What happens when you run the following program? Why do we get that result?

def set_foo():
    foo = 'bar'

set_foo()
print(foo)

# This will generate an erro because 'foo' is defined in the function, 
# and not accessible outside of it.